// Placeholder — UI tests for the menu-bar app are limited because the
// widget tree depends on tray_manager, window_manager, hotkey_manager and
// app_links plugins, all of which are platform-channel-only. The OAuth
// helpers in lib/oauth/ have unit-testable surface and would be the next
// place to add tests if needed.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
