# Cloudflare Pages config. Most projects don't need this — the dashboard
# settings (build command / output dir) are sufficient. Included for users
# who prefer to deploy via `wrangler pages deploy ./dist`.

name = "stohr-site"
pages_build_output_dir = "./dist"
compatibility_date = "2025-01-01"
