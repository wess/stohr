# Cloudflare Workers (Static Assets) config. Created from the unified
# "Workers + Pages > Connect to Git" flow, which provisions a Workers
# project (not a legacy Pages project). Deploy with `wrangler deploy`.

name = "stohr"
compatibility_date = "2025-01-01"

[assets]
directory = "./dist"
not_found_handling = "404-page"
